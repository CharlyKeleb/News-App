class Articles{
  String title;
  String description;
  String urlToImage;
  DateTime publishedAt;
  String content;
  String url;

  Articles({ this.title, this.description, this.urlToImage, this.publishedAt, this.content, this.url});
}